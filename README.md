# Project for Automation Task

## Installation
* Download the project.
* Install gems
```bash
bundle install
```

# Launch
* To run Login UI tests
```
bundle exec rspec spec/login_spec.rb
```
* To run API tests
```
bundle exec rspec spec/api_spec.rb
```

# Recursive (Separate task)
* Check comments in **recursive.rb**